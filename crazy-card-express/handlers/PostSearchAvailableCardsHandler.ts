export async function PostSearchAvailableCardsHandler () {
    
}

const filterCardsByRequirment = (userData: PostSearchAvailableCardsRequestBody): Cards[] => {
    //Can user have liquid card
    //! income > 16k 
    //! isGoodEmploymentStatus === 'partTime' || 'full-time' || 'student'
    //Can user have Student card 
    //! Is user a student? 
    //Any user can have anywhere card
    //!anyone
}
